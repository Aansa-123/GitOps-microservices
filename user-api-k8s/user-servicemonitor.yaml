apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-api
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: user-api

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s