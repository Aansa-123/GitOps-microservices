apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: default
  labels:
    app: user-api
spec:
  selector:
    app: user-api
  ports:
    - name: metrics
      port: 3000
      targetPort: 3000
  type: ClusterIP