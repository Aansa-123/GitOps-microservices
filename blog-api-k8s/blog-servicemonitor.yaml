apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: blog-api
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: blog-api

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s