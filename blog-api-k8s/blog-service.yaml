apiVersion: v1
kind: Service
metadata:
  name: blog-service
  labels:
    app: blog-api
spec:
  selector:
    app: blog-api

  ports:
    - name: metrics
      port: 3001
      targetPort: 3001

  type: NodePort