apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: invoice-api
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: invoice-api

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s